import {NextRequest,NextResponse} from "next/server";import crypto from "crypto";import {connectNifty} from "@/src/lib/niftyMcp";
export async function GET(req:NextRequest){try{const origin=new URL(req.url).origin;const state=crypto.randomBytes(24).toString("hex");const r=await connectNifty(origin);if(!r.authorizationUrl)return NextResponse.redirect(new URL("/nifty?connected=1",origin));const u=new URL(r.authorizationUrl);u.searchParams.set("state",state);const res=NextResponse.redirect(u);res.cookies.set("nifty_oauth_state",state,{httpOnly:true,secure:true,sameSite:"lax",path:"/",maxAge:600});return res}catch(e:any){return NextResponse.redirect(new URL("/nifty?error="+encodeURIComponent(e.message),req.url))}}