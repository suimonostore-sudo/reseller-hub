import {NextResponse} from "next/server";import {prisma} from "@/src/lib/prisma";
export async function GET(){const [inventory,listings,sales,needsMatch,readyToPick,labels]=await Promise.all([prisma.inventoryItem.count(),prisma.listing.count(),prisma.sale.count(),prisma.sale.count({where:{status:"NEW"}}),prisma.sale.count({where:{status:"MATCHED"}}),prisma.shippingLabel.count()]);let unlisted=0;try{const x=await prisma.inventoryItem.aggregate({where:{unlistedQuantity:{gt:0}},_sum:{unlistedQuantity:true}});unlisted=x._sum.unlistedQuantity||0}catch{}return NextResponse.json({inventory,listings,sales,needsMatch,readyToPick,labels,unlisted});}